/**
 * reference-audio-path.js
 * ============================================================
 * المسار الأساسي لملفات مكتبة الاستماع المرجعية (القرار 9.2) — قيمة نصية
 * ثابتة يعتمد عليها `build/build.mjs` بالضبط (`.replaceAll` حرفي على هذا
 * السطر) لإعادة كتابته لمسار مخرجات الدمج الصحيح. **لا تُغيَّر هذي القيمة
 * بلا تحديث `build/build.mjs` بالتوازي** — التطابق الحرفي مطلوب.
 *
 * القيمة هنا صحيحة وقت التطوير (تشغيل من `src/ui/pages/XX-name/` عبر خادم
 * محلي) — 4 مستويات أعلى لتصل لجذر المستودع، ثم داخل `data/`.
 */
export const REFERENCE_AUDIO_BASE = "../../../../data/reference-library/audio/";

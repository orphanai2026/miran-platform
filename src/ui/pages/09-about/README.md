# 09 — من نحن

صفحة عامة مستقلة (القرار 9.5) — وصف المشروع، الفلسفة التقنية، رابط
المستودع، وقائمة المساهمين/الخبراء. على نمط قسم `#more` في RECORD-N.

جزء من التنقّل الحي الثماني — أخذت مكان صفحة #7 (الإعدادات) في `nav.js`
بعد إزالتها (القرار 9.6). العدد الإجمالي للروابط في شريط التنقّل يبقى 8.

## البنية

| الملف | الدور |
|---|---|
| `index.html` | الصفحة نفسها |
| `about-page.js` | المحتوى الثابت + عرض قائمة المساهمين |
| `contributors-data.js` | بيانة المساهمين — **تُحدَّث يدويًا** من المالك (لا مصدر تلقائي، انظر تعليق الملف) |

## اختبار

```bash
node src/ui/pages/09-about/tests/page-smoke.test.mjs
```

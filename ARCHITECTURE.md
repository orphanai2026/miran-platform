# ARCHITECTURE.md — مِران (منصة تعليم الناي الموحدة)

وثيقة معمارية إلزامية من اليوم الأول (قرار معماري، القسم 4 من سجل القرارات). تحدد الملفات
الحساسة والآمنة للتعديل، وتُحدَّث مع كل مرحلة تنفيذ جديدة.

## المبدأ الحاكم

فصل كامل بين بيئة التطوير (ملفات منظّمة حسب المسؤولية) وبيئة التشغيل (ملف واحد نهائي بلا خادم
وبلا اعتماديات، ينتج من خطوة دمج بسيطة في `build/` وقت النشر فقط). هذا الفصل هو الضمانة التي
تحفظ الوعد الأصلي لمِران (ملف واحد، بلا خادم، بلا اعتماديات) للمستخدم النهائي.

## خريطة المسؤوليات

| المجلد | المسؤولية | مصدر القرار |
|---|---|---|
| `src/exercises/` | تمارين الـ74 يومًا كما هي | القسم 4 — تُنقل بلا إعادة بناء |
| `src/calibration/` | معايرة، تسجيل، مرجع نغمة شخصي | القرار 1، 2، 3 |
| `src/metronome/` | محرك مترونوم واحد، مُستخدَم في صفحتين | القسم 7، الصفحة #5 |
| `src/maqamat/` | سلم + سِّير + مصادقة سمعية | القرار 5 |
| `src/ui/pages/` | الصفحات الثماني المعتمدة | القسم 7 |
| `src/ui/shared/` | ملفات مشتركة بين أكثر من صفحة (التنقّل العام حاليًا) | القسم 7 |
| `data/` | مصدر الحقيقة الواحد (سلالم، مقامات، سماحية) | القسم 4 |
| `cloudflare/` | نقطة استقبال JSON — الاستثناء الوحيد لمبدأ "لا خادم" | القرار 4 |
| `build/` | خطوة الدمج وقت النشر فقط | القسم 4 |

## ملفات حساسة (لا تُعدَّل إلا بقرار صريح جديد في سجل القرارات)

- `src/exercises/**` — منقول من مِران الأصلي بعد نجاح حقيقي (خفّض الفشل من 170+ إلى 13 حالة
  بعزل الإيقاع). أي تعديل بنيوي هنا يحتاج قرارًا صريحًا، وليس مجرد تحسين.
- `data/**` — مصدر الحقيقة الواحد؛ أي تكرار أو مصدر بديل للبيانة نفسها ممنوع.
- منطق اعتماد المرجع الشخصي للنغمة (القرار 1: 15 محاولة/3 أيام أو جلسة واحدة للمحترف، موافقة
  صريحة لا تلقائية، لقطة ثابتة بعد الاعتماد).

## ملفات آمنة للتعديل الحر (ضمن مرحلة تنفيذ محددة)

- `src/ui/pages/**` — واجهة كل صفحة، طالما لا تُغيّر منطق البيانات الأساسي.
- `src/metronome/**`، `src/calibration/**`، `src/maqamat/**` — بناء جديد بروح RECORD-N، حر
  التصميم طالما التزم بالقرارات 1-6.

## الاستثناء الوحيد لمبدأ "لا خادم"

نقطة استقبال JSON بسيطة (سب دومين على Cloudflare Workers/KV) لاستقبال بيانات العازفين المُصدَّرة
— القرار 4. لا يُوسَّع هذا الاستثناء لأي غرض آخر إلا بفتح نقاش الحسابات/الاشتراكات (القسم 6)
صراحة.

**الحالة: الكود مكتوب ومُختبر محليًا بالكامل (`cloudflare/intake-worker/`)، والجانب العميل مدموج
فعليًا في صفحة #7 (`intake-sync.js`) — لكن النشر الفعلي على Cloudflare لم يحدث بعد.** بيئة تنفيذ
Claude ليس لديها وصول شبكي لنطاقات Cloudflare ولا بيانات اعتماد حساب Cloudflare — النشر خطوة
يدوية يجب أن ينفّذها المالك بنفسه (خطوات كاملة في `cloudflare/intake-worker/README.md`). قبل
النشر وإدخال الرابط الفعلي في صفحة #7، ميزة المزامنة الشبكية خامدة بالكامل بتصميمها — لا رابط
افتراضي مُضمَّن في الكود.

## النشر للمراجعة (GitHub Pages)

`.github/workflows/deploy-pages.yml` يشغّل خطوة الدمج (`build/build.mjs`) تلقائيًا عند كل دفع
لـ `main`، وينشر مخرجاتها (`build/dist/`) على GitHub Pages — لا يُدفع أي ناتج دمج للمستودع نفسه
(`dist/` مستثنى في `.gitignore`، يبقى مصدر الحقيقة الوحيد هو `src/`). صفحة الجذر تُحوَّل تلقائيًا
للصفحة الرئيسية (`01-home/`) عبر `build/root-redirect.html`. هذا للمراجعة البصرية والاختبار
اليدوي فقط — لا علاقة له باستثناء القرار 4 (نقطة استقبال JSON على Cloudflare تبقى منشورة يدويًا
من المالك بشكل منفصل).

**تفعيل لمرة واحدة مطلوب من المالك:** إعدادات المستودع على GitHub ← Settings ← Pages ← Source:
اختيار "GitHub Actions" (لا "Deploy from a branch"). بعدها كل دفع لـ `main` ينشر تلقائيًا.

## سجل تحديثات هذه الوثيقة

- **[التاريخ الحالي]** — إنشاء أولي: بنية المجلدات الثماني + قواعد الملفات الحساسة/الآمنة، دون
  كود تنفيذي بعد.
- **[تحديث لاحق]** — تفكيك `src/exercises/legacy-miran/index.html` فعليًا (كان 4255 سطرًا في
  `<script>` واحد): منطق المنهج/محرك الصوت/التمارين نُقل حرفيًا (تحقق `diff` بايتي مطابق) إلى
  `src/exercises/legacy-miran/js/*.js`، وقسم `CALIBRATION` القديم عُزل فيزيائيًا في
  `src/exercises/legacy-miran/legacy-calibration-do-not-reuse/old-calibration.js` — لا يزال
  محمَّلاً ويعمل (تحتاجه شاشة المعايرة القديمة في الواجهة حاليًا)، لكنه معزول بوضوح ومُعلَّم بأنه
  **لا يُعتمَد كأساس** لأي بناء جديد. `index.html` نفسه صار غلاف HTML فقط يحمّل نفس الملفات بنفس
  الترتيب عبر `<script src>` عادية (لا وحدات ES) للحفاظ على نفس سلوك النطاق العام. خط الأساس
  9/9 ناجح قبل وبعد. التفاصيل الكاملة في `src/exercises/legacy-miran/SPLIT-NOTES.md`.
- **[تحديث لاحق]** — بناء أول صفحة فعلية من خريطة الصفحات الثماني: `src/ui/pages/02-calibration/`
  (المعايرة الشخصية)، بربط `src/calibration/` (كشف نغمة + منطق القرارات 1-3) مع
  `src/metronome/` (النسخة المصغّرة) في واجهة تعمل فعليًا (تسجيل ميكروفون حي، اقتراح/اعتماد
  مرجع، استثناء تعليم الاسم من عينة واحدة، تخزين محلي جديد في `sample-store.js`). لا لمس ولا
  إعادة استخدام لأي كود من `src/exercises/legacy-miran/` بما فيها `legacy-calibration-do-not-reuse/`.
  أُضيفت دوال تصدير/استيراد عامة جديدة لـ `PersonalReferenceStore` (إضافات فقط، لا تعديل على
  المنطق القائم) بدل الوصول المباشر لحقولها الداخلية من الواجهة. كل الاختبارات القائمة (خط أساس
  مِران القديم 9/9، `calibration-engine` 16/16، `pitch-detector` 7/7، `metronome-engine` 10/10،
  `maqam-schema` 17/17) ما زالت ناجحة، بالإضافة لاختبار دخان جديد للصفحة (5/5).
- **[تحديث لاحق] خريطة الصفحات الثماني مكتملة بالكامل** — بناء الصفحات المتبقية جميعًا، كل واحدة
  في مرحلة منفصلة بدفع مستقل، بنفس انضباط "ميزة واحدة لكل إصدار + اختبار رجعي كامل قبل كل دمج":
  - **صفحة #5 (المترونوم المستقل):** ربط `metronome-ui-full.js` الجاهز مسبقًا مباشرة، بلا طبقة
    منطق إضافية — نفس محرك صفحة #2، واجهة مختلفة فقط (مصدر حقيقة واحد للكود).
  - **صفحة #4 (دليل المقامات):** عرض `ALL_MAQAMAT` من `src/maqamat/` مباشرة (سلم + سِّير)، مع
    علامة "؟" صريحة لكل سِّير غير مصادَق عليه (كلها حاليًا، القرار 5) — عرض/قراءة فقط، بلا إرشاد
    صوتي تفاعلي (يحتاج دمجًا مستقبليًا مع `src/calibration/`).
  - **صفحة #6 (المكتبة/التصدير):** إعادة استخدام `sample-store.js` من صفحة #2 (استيراد عابر
    للصفحات، لا تكرار منطق تخزين) لعرض حالة كل نغمة وتصدير JSON فعلي (Blob + رابط تنزيل) — بلا
    إرسال شبكي فعلي (ذلك موضوع صفحة #7/استثناء القرار 4).
  - **صفحة #8 (الدليل التعليمي):** صفحة HTML/CSS ثابتة بالكامل، **بلا JavaScript إطلاقًا** —
    مطابقة حرفية لنص القرار 6 ("نص إرشادي ثابت، لا تفاعلي")، تعرض الطبقات الثلاث وتوصية صريحة
    بمعلم بشري عند الطبقة الثالثة.
  - **صفحة #7 (الإعدادات + المزامنة):** أول ظهور لمعرّف المستخدم المحلي (`local-user-id.js`،
    مفتاح `localStorage` جديد منفصل تمامًا) — تحصين بيانات القسم 6 بلا فتح قرار الحسابات الكامل،
    ويُستخدَم لاحقًا كـ"كود مزامنة". إعدادات السماحية (حدود القرار 2) عرض فقط، لا تعديل، لتفادي
    فتح منطق القرار 2 الحسابي بلا طلب صريح.
  - **صفحة #3 (التمارين):** **ليست إعادة بناء** — نقطة دخول رقيقة (`meta refresh`، بلا JS) تُوجّه
    فورًا لـ `src/exercises/legacy-miran/index.html` المحمي والمُختبر أصلًا. القرار الصريح
    "بلا تعديل بنيوي" استُخدم حرفيًا لتبرير عدم إعادة البناء. **خطأ اكتُشف ب اختبار تنقّل متصفح
    فعلي (لا فحص نص فقط) وصُحِّح قبل الدفع:** المسار النسبي الأول كان بمستويين فقط (`../../`)
    فأدى لـ 404 حقيقي؛ صُحِّح لثلاثة مستويات (`../../../`) مطابقًا لعمق استيراد صفحة #2.
  - **صفحة #1 (الرئيسية، آخر صفحة):** تجمع ملخص تقدّم من ثلاثة مصادر مقروءة بلا تكرار: تقدّم
    المنهج (قراءة مباشرة لمفتاح `localStorage` الخام `miran_prog` الذي يكتبه `persistence.js`
    المحمي — **قراءة بيانات لا استيراد كود**)، ملخص المعايرة (إعادة استخدام صفحة #2)، والمعرّف
    المحلي (إعادة استخدام صفحة #7). تبني شبكة دخول سريع لكل الصفحات السبع، مع تمييز صفحة
    التمارين كرابط رئيسي ("المرحلة الحالية"، مطابقةً لنص القسم 7). **خطأ اكتُشف ب اختبار بيانات
    مزروعة وصُحِّح قبل الدفع:** حساب "المراجع المعتمَدة" نسي استدعاء `restoreFrozenSnapshot` عند
    إعادة بناء المخزن، فكان يرجع صفرًا دائمًا.

  **النتيجة الإجمالية:** الانحدار الكامل عبر كل الصفحات والمحركات — 96/96 اختبارًا ناجحًا
  (خط الأساس القديم 9/9، صفحة #1: 6/6، صفحة #2: 5/5، صفحة #3: 2/2، صفحة #4: 5/5، صفحة #5: 6/6،
  صفحة #6: 4/4، صفحة #7: 5/5، صفحة #8: 4/4، `calibration-engine` 16/16، `pitch-detector` 7/7،
  `metronome-engine` 10/10، `maqam-schema` 17/17). خريطة الصفحات الثماني (القسم 7) مكتملة
  بالكامل. المتبقي الصريح من سجل القرارات: تنقّل عام موحَّد بين الصفحات (لم يُطلب صراحة بعد)،
  استثناء القرار 4 الفعلي (نقطة استقبال Cloudflare)، والهوية البصرية والحسابات/الاشتراكات
  (مؤجَّلان صراحة، القسمان 5 و6).
- **[تحديث لاحق] تنقّل عام موحَّد بين الصفحات** — أول ملف في `src/ui/shared/` (لم يكن موجودًا قبل
  هذي المرحلة): `nav.js` يبني شريط تنقّل بروابط للصفحات الثماني كلها، يُبرز الصفحة الحالية.
  مدموج في كل صفحة عبر JS من الصفحات السبع المبنية سابقًا (استيراد `mountNav` بمسار نسبي موحَّد
  `../../shared/nav.js`، ثابت لأن كل الصفحات أخوات بنفس العمق). **صفحة #8 استثناء مقصود:** نسخة
  HTML ثابتة يدويًا من نفس البنية والأصناف، بلا استيراد `nav.js`، حفاظًا على قرارها الصريح "بلا
  JavaScript إطلاقًا" (القرار 6). **صفحة #3 (إعادة التوجيه) وصفحة المنهج المحمية أنفسهما بلا
  شريط** — الأولى تنقّل لحظي لا فائدة لشريط فيه، والثانية ملف محمي لا يُعدَّل بلا قرار صريح.
  اختبار مشترك جديد (`src/ui/shared/tests/nav-smoke.test.mjs`، 4/4 ناجح) يغطي الروابط الثمانية،
  تمييز الحالة على صفحتين ممثلتين، تنقّلًا فعليًا حقيقيًا بين صفحتين، ونسخة صفحة #8 الثابتة.
  الانحدار الكامل بعد الإضافة: **100/100 ناجح** — لا كسر لأي اختبار قائم.
- **[تحديث لاحق] استثناء القرار 4 مبني فعليًا (كود مكتمل، نشر يدوي متبقٍّ):**
  - **`cloudflare/intake-worker/worker.js`:** نقطة استقبال Workers/KV — `validatePayload` (تحقق
    بنيوي)، `extractPlayerId` (من مسار `/submit/{playerId}`)، ومعالج `fetch` كامل. **تخزين
    تراكمي** (مفتاح `player:{id}:{receivedAtMs}` منفصل لكل إرسال، لا استبدال). نطاق ضيق مقصود:
    استقبال وتخزين فقط، لا قراءة/تحليل. 16 اختبار وحدة خالص بـ Node (بلا Miniflare/wrangler،
    بالاعتماد على `Request`/`Response` المدمجة + KV وهمي بسيط) — 16/16 ناجح.
  - **`wrangler.toml` + `README.md` للنشر اليدوي:** بيئة تنفيذ Claude بلا وصول شبكي لنطاقات
    Cloudflare وبلا بيانات اعتماد الحساب — **لم يحدث نشر فعلي**، فقط كود جاهز وخطوات كاملة
    موثَّقة للمالك لينفّذها بنفسه (`wrangler login` → `kv:namespace create` → `wrangler deploy`).
  - **الجانب العميل (`src/ui/pages/07-settings-sync/intake-sync.js`):** `getEndpointUrl`/`setEndpointUrl`
    (تخزين محلي لرابط اختياري بالكامل، فارغ افتراضيًا)، `attemptSubmit`، و`SyncManager` (إرسال
    تلقائي عند تحميل الصفحة إن كان الرابط مُهيَّأ + إعادة محاولة تلقائية عند حدث `online` بعد فشل
    شبكي — نص القرار 4 حرفيًا). **بلا رابط مُدخَل، الميزة خامدة بالكامل.**
  - **استخراج مشترك:** `rebuildStore`/`buildExportPayload` نُقلا من `library-page.js` (صفحة #6)
    إلى ملف جديد `src/ui/pages/06-library-export/export-payload.js`، تستورده الآن الصفحتان #6
    و#7 — مصدر حقيقة واحد لبنية حمولة التصدير بين التصدير اليدوي والإرسال التلقائي.
  - صفحة #7 مُحدَّثة: قسم "نقل البيانات" صار تفاعليًا فعليًا (رابط + حفظ + إرسال الآن + عرض
    حالة)، لا شرحًا نصيًا فقط. 10 اختبارات دخان جديدة/مُحدَّثة (كانت 5) — تدفق كامل بـ `page.route`
    (بلا اتصال فعلي بأي خادم حقيقي): خامد بلا رابط، إرسال ناجح مع تحقق فعلي من الطلب والحمولة،
    فشل شبكي، وإعادة محاولة تلقائية حقيقية عند `online` بلا تدخّل يدوي.
  **الانحدار الكامل بعد الإضافة: 121/121 ناجح** (كل الأرقام السابقة + `cloudflare worker` 16/16 +
  صفحة #7 صارت 10/10 بدل 5/5) — لا كسر لأي اختبار قائم.
- **[تحديث لاحق] خطوة الدمج (`build/build.mjs`) مبنية فعليًا — تسدّ فجوة كانت `build/` فيها مجرد
  README نائب منذ اليوم الأول.** المشكلة التي عالجتها: كل الصفحات الست المبنية بوحدات ES تحتاج
  خادمًا محليًا لتعمل (المتصفحات تمنع وحدات ES عبر `file://`) — يخالف الوعد التأسيسي "بلا خادم،
  بلا اعتماديات تشغيل" لأي مستخدم نهائي يفتح صفحة بالنقر المزدوج مباشرة.
  - **أداة دمج مكتوبة يدويًا بالكامل، بلا esbuild/rollup/أي حزمة npm** — القرار 4 ينص صراحة "بلا
    مكتبات خارجية". تحلّل نصيًا أسطر `import`/`export` (كل الاستيرادات في المشروع بصيغة واحدة
    بسيطة، تحقَّق منه فعليًا)، ترتّب التبعيات طوبولوجيًا، تُلحقها في `<script>` واحد داخل IIFE،
    **تكتشف تعارض الأسماء تلقائيًا** بدل إنتاج كود مكسور بصمت.
  - **تفسير صريح لـ"ملفًا واحدًا نهائيًا" — سُئل المالك صراحة وأكَّد (تفصيل كامل في
    `build/README.md`):** بما أن خريطة الصفحات الثماني (القسم 7) قرار مُقفَل ينص على ثماني صفحات
    منفصلة، فُسِّر "ملف واحد" هنا على مستوى **كل صفحة على حدة** (8 ملفات مستقلة تمامًا). **المالك
    أكَّد أن هذا هو التصميم النهائي المقصود** — صفحات منفصلة أسهل صيانةً وتعديلًا مستقبلًا، لا خيار
    مؤقت بانتظار مراجعة.
  - **الاختبار الحقيقي (`build/tests/build-output.test.mjs`):** يفتح كل صفحات `build/dist/` عبر
    `file://` مباشرة — **بلا أي خادم HTTP إطلاقًا**، بخلاف كل اختبارات المصدر الأخرى في المستودع.
    9/9 ناجح: كل الصفحات الثماني، تفاعل فعلي (مترونوم، تبديل مقام)، وتنقّل فعلي حقيقي بالنقر بين
    صفحتين مختلفتين يبقى `file://` طوال الوقت. **هذا الدليل الفعلي أن الوعد التأسيسي محقَّق الآن،
    لا نظريًا فقط.**
  - `build/dist/` غير مُتتبَّع في git (مشمول في `.gitignore` عبر نمط `dist/` الموجود مسبقًا).
  **الانحدار الكامل بعد الإضافة: 130/130 ناجح** (121 السابقة + `build output` 9/9 الجديدة) — لا
  كسر لأي اختبار مصدر قائم.
